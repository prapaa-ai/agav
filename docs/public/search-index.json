[
  {
    "id": "features/agent-marketplace",
    "title": "Agent Marketplace",
    "content": "Agent Marketplace The agent marketplace is a catalog of community-built agents hosted on GitHub. Agav reads the catalog at startup (when you open the Marketplace tab) and lets you install agents directly into your global or project configuration. Opening the marketplace The marketplace tab fetches from the configured marketplace URL and displays all available agents. Browsing agents Agents are displayed three per page. Navigate with: | Key | Action | | --------- | ------------------------------------------------------------------ | | / | Move selection within the current page | | / | Previous / next page | | | Open search — filter by name, description, category, or tag | | | Inspect selected agent (shows tools, credentials, and description) | | | Refresh from the marketplace URL | | | Clear search / exit | Agents already installed on your system show a or badge. Installing an agent Select an agent and press . If the agent is not yet installed, a destination prompt appears: Press or to install. If the agent is already installed at project scope, pressing offers to promote it to global (removes the project copy and reinstalls globally). Globally installed agents block re-installation at project scope — use the List tab to manage them instead. Configuring after install After installing, switch to the List tab to configure credentials: The config editor shows each required credential with its current status ( or ). Press on a field to edit it, paste your value, and press again to save. Values are encrypted at rest. CLI installation Install without opening the TUI: Local marketplace For local development and testing, clone the marketplace repo and set: The environment variable overrides the config file value and the built-in default. Hosting a marketplace Any GitHub repository (or static file server) can act as a marketplace. The minimum structure: See the official marketplace repository for a complete example. Deleting agents When a user-created agent (one without a marketplace ) is deleted from the List or Create tab, Agav automatically saves it as a template before removal. The template preserves the agent's name, description, system prompt, MCP server selections, and tags. You can restore it later from the Create tab (). Marketplace agents (those installed from a URL) are deleted without creating a template — they can be reinstalled from the marketplace at any time. Related - Agents — how agents work - Install and Configure an Agent — step-by-step guide - Agent Manifest — AGENT.md format reference",
    "slug": "features/agent-marketplace"
  },
  {
    "id": "features/agents",
    "title": "Agents",
    "content": "Agents Agents are named, purpose-built sub-agents that extend Agav with domain-specific capabilities. Each agent has its own tool set, system prompt, and isolated credentials. When your query involves a task an agent handles — listing GitHub PRs, searching Jira tickets, taking a screenshot — the LLM routes to it automatically. How agents work When Agav starts, it loads all enabled agents and registers each one as a callable tool. The LLM reads a compact catalog (one line per agent, capped at ~500 tokens total) and chooses the right agent for the task. An agent invocation runs a nested agent loop with the agent's tools, system prompt, and credentials — fully isolated from the parent session. Results stream back to you normally. Loading order Agav discovers agents from three locations. Later tiers override earlier ones by name, so you can replace a bundled agent with a custom version: | Tier | Location | Override priority | | --- | --- | --- | | Bundled | Shipped with Agav binary | Lowest | | Global | | Middle | | Project | in the working directory | Highest | Installing agents from the marketplace Browse and install agents from the community marketplace: See Agent Marketplace for full details. Creating agents You can create custom agents directly from the TUI without writing a manifest by hand: The Create tab opens a \"My Agents\" hub that lists your user-created agents and any saved templates. From here you can create a new agent or restore one from a template. Wizard flow Selecting New Agent launches a four-step wizard: 1. Name & Description — pick a unique agent name and a short description of what it does. 2. System Prompt — write the agent's system prompt, or press to auto-generate one. The LLM uses your name and description to draft a prompt you can edit before continuing. 3. MCP Servers — select which MCP servers the agent should have access to. The list is populated from your workspace config ( in or ). 4. Review & Save — preview the full agent definition and confirm. The agent is saved to and immediately available for use. Templates When you delete a user-created agent, Agav automatically saves it as a template so you can recreate it later without starting from scratch. - Templates appear in the Create tab's \"My Agents\" list with a label. - Opening a template pre-populates the wizard with the original name, description, system prompt, and MCP server selections — edit anything before saving. - Press on a template in the list to remove it permanently. - Templates are stored in . Per-agent MCP servers Agents can declare MCP servers in their manifest. Each server entry supports an field for passing environment variables (API keys, base URLs, etc.) to the server process. Servers are started when the agent runs and stopped when it finishes — they are fully scoped to the agent's lifecycle and do not affect the parent session or other agents. Credentials Each agent declares the environment variables it needs ( in the manifest). Credentials are: - Stored encrypted in — never in plain text - Injected into only during that agent's execution — not shared with other agents or the parent session - Editable from the agent inspect view: Agents with missing credentials show a indicator in the List tab. The LLM receives an error message if it tries to call an unconfigured agent. Tool permissions and confirmation Every tool in an agent is classified as either (read-only) or (creates, edits, or deletes data): - Safe tools run without a confirmation prompt. - Modifies tools pause and display a confirmation before executing. The classification is declared in the agent manifest () and is visible in the inspect view. Agent tools also run inside the OS-level sandbox when one is available (Seatbelt on macOS, Bubblewrap on Linux). This applies the same filesystem, network, and credential isolation described in Security — Shell sandbox to every agent tool execution, not just shell commands. Bundled agent tools are excluded from sandboxing since they ship with Agav. Model and effort overrides Agents inherit the session's model and effort by default. You can override them per-agent in the config editor (). The override is stored in the agent's and shown as in the inspect view. Clear the value to revert to inheriting from the session. CLI TUI Open to manage agents interactively. The three tabs are: - * — view, inspect, enable/disable, and remove installed agents. - — browse and install community agents. - * — create new agents with the wizard, and manage your agents and templates. Related - Agent Marketplace — browse and install community agents - Agent Manifest reference — full AGENT.md format - Build a Native Agent — write your own agent - Install and Configure an Agent — step-by-step walkthrough - Build an Agent from a Template — restore and customize a saved template",
    "slug": "features/agents"
  },
  {
    "id": "features/built-in-tools",
    "title": "Built-in Tools",
    "content": "Built-in Tools Agav exposes these tools to its agent loop. | Tool | Purpose | | --- | --- | | | Read text ranges and preview images, PDFs, and Office documents | | | Create or replace a file | | | Replace one exact, unique string in a file | | | Find names and glob matches | | | Search file contents with regular expressions | | | List a directory | | | Map a repository's directory and symbol structure | | | Execute a shell command with sandbox detection | | | Detect and run a supported project test framework | | | Request definitions, references, or hover data from a language server; diagnostic notifications are not yet fully surfaced | | | Read Jupyter notebook cells | | | Change a notebook cell or its type | | | Search the web with DuckDuckGo | | | Send an HTTP request and return the response | | | Create or view GitHub pull requests and issues | | | Update the active plan's current step | | | Persist durable project or user context | | | Delegate an independent task | Connected MCP servers, installed plugins, and active skills can add more tools. Language-server navigation The tool can find definitions, references, and hover information in TypeScript, JavaScript, Python, Rust, and Go projects when the matching language-server executable is available. | Language | Executable | | --- | --- | | TypeScript or JavaScript | | | Python | | | Rust | | | Go | | Ask Agav for semantic evidence explicitly when a text search is not enough: Agav combines language-server results with file reads and repository search. If the server is unavailable, confirm that its executable is on in the shell that started Agav. Diagnostic notifications are not yet fully surfaced, so use the project's normal diagnostic command when complete output matters. Confirmations and undo Writes, edits, and sensitive commands can display a confirmation prompt. Edit confirmations include a diff. Choosing the session-wide approval option releases queued confirmations as well. Agav records prior content for tracked file edits in an in-memory undo stack: The stack contains up to twenty changes and does not survive process exit. A newly created file has no previous content to restore. File boundaries The file tools (, , ) enforce path boundary checks. Writes are restricted to the working directory, temp directory, and . Reads are denied for credential stores (, , , ). Writes to and inside the project are always denied. See security for details. Shell execution Shell commands have a 30-second default timeout and bounded output. Agav auto-detects macOS Seatbelt or Linux Bubblewrap; Docker can be requested as a tool override. The Seatbelt and Bubblewrap backends deny network access and restrict filesystem writes to the working directory. When no backend is available, the command runs unsandboxed with secret-like environment variables filtered out. Use to refuse startup without a sandbox. Review security before using auto-accept mode.",
    "slug": "features/built-in-tools"
  },
  {
    "id": "features",
    "title": "Features",
    "content": "Features Agav combines a core toolset with extension points that add reusable knowledge and external capabilities. - Built-in tools cover files, search, shell, testing, language servers, notebooks, GitHub, web access, plans, and memory. - Skills package reusable instructions and tool policies, with a built-in marketplace, validation, and automatic improvement. - MCP connects stdio subprocess servers and remote HTTP/SSE endpoints that expose tools, resources, and prompts. - Plugins and subagents add JavaScript tools and parallel task execution. - Agents are specialized sub-agents with scoped tools, credentials, and system prompts. - Agent Marketplace provides community-built agents for cloud platforms, developer workflows, and automation. Tools are chosen by the model based on the task. Read-only operations can run directly; sensitive operations follow the active permission mode and may require confirmation.",
    "slug": "features"
  },
  {
    "id": "features/mcp",
    "title": "Model Context Protocol",
    "content": "Model Context Protocol Agav connects to MCP servers over two transport families: stdio (local subprocesses communicating through newline-delimited JSON-RPC over stdin/stdout) and remote (HTTP or SSE endpoints). Stdio servers Add to or : Each server supports a command, optional argument list, and optional environment overrides. Restart Agav after changing server configuration. Remote servers (HTTP / SSE) Remote servers connect to an MCP endpoint over the network instead of spawning a local process. Two transport modes are supported: - Streamable HTTP – the current MCP spec transport. Uses a single HTTP endpoint for both requests and server-initiated messages. - Legacy SSE – the older Server-Sent Events transport. Uses one SSE connection for server-to-client messages and a separate HTTP POST endpoint for client-to-server messages. When is omitted, Agav auto-detects: it tries Streamable HTTP first and falls back to Legacy SSE if the server does not support it. Basic configuration is optional when is present — Agav infers the server type automatically. Explicit transport selection If you know which transport the server speaks, pin it with : Remote server config fields | Field | Type | Required | Description | |-------|------|----------|-------------| | | string | yes | HTTP/SSE endpoint URL | | | | no | Explicitly marks the server as remote; inferred when is present | | | or | no | Force a specific transport; omit to auto-detect | | | Record\\ | no | Extra headers sent with every request | are sent with every request and are the recommended way to pass authentication tokens. The , , and fields are not used for remote servers. Exposed capabilities - Tools are registered in the main tool pool with their server name in the description. - Resources are summarized in model context and read on demand through . - Prompts become slash commands. Agav requests the rendered prompt from the owning server and submits its messages to the conversation. If one server fails to start, Agav continues without it. Run to see connected server names and counts for discovered resources and prompts.",
    "slug": "features/mcp"
  },
  {
    "id": "features/plugins-and-subagents",
    "title": "Plugins and Subagents",
    "content": "Plugins and Subagents Local plugins Agav loads and files from . A module can default-export one tool or export a array. Each tool needs , , , and an async function returning . Broken plugin modules are skipped. Plugins execute local JavaScript with the same operating-system access as Agav, so install only code you trust and restart Agav after changes. Parallel subagents The tool delegates a clear, self-contained task with a short UI title. Subagents: - have independent conversation context - inherit the active model, effort, system prompt, steers, permission mode, and available tools - cannot create nested subagents - share confirmations through the main terminal UI - report streaming progress and token usage - allow up to five concurrent tasks Tasks whose description indicates file changes attempt to use an isolated Git worktree. Agav applies the resulting changes back to the original checkout and reports a warning if that merge step fails. Read-only tasks use the current working directory directly. Give a subagent all necessary paths, constraints, and expected output because it is instructed to work without follow-up questions.",
    "slug": "features/plugins-and-subagents"
  },
  {
    "id": "features/skills",
    "title": "Skills",
    "content": "Skills Skills are reusable instruction packages that extend Agav with domain-specific procedures. Each skill is a file — YAML front-matter declaring metadata, followed by a Markdown body containing the instructions the LLM follows when the skill is activated. Skills let you encode repeatable workflows (code review, test generation, research) and share them across projects and teams. Loading order Skills are loaded from three tiers. A skill in a later tier with the same slug replaces the earlier definition: | Tier | Location | Override priority | |------|----------|-------------------| | Bundled | Shipped with Agav binary | Lowest | | Global | | Middle | | Project | in the working directory | Highest | When a project skill overrides a bundled or global skill, the override is tracked and shown as in and . If two skills in the same tier share a slug, the second is skipped with a warning. Skills whose slug collides with a built-in command name (e.g. , , ) are also silently skipped. Bundled skills are compiled directly into the binary, so they work even when running from a compiled build where file system paths are unavailable. Bundled skills Agav ships with 10 bundled skills: | Skill | Description | |-------|-------------| | | Review code changes for bugs, security issues, and improvements | | | Multi-source research on a topic with citations | | | Diagnose and fix errors and bugs | | | Generate documentation for code | | | Explain code in plain language | | | Generate a commit message from staged changes | | | Suggest and apply code refactoring | | | Check code for security vulnerabilities | | | Reduce complexity and simplify code | | | Generate unit tests for existing code | Using skills Skills can be invoked in two ways depending on their setting: - * — called explicitly as a slash command: - — selected automatically by the LLM when a query matches the skill's triggers - — available both ways Automatic dispatch When a user message starts with , Agav immediately activates that skill without planning. This is used internally by the LLM for automatic skill selection. Dynamic context variables Skill bodies support two placeholder variables that are replaced at runtime: - — replaced with the user's arguments, or if none - — replaced with the current working directory Steer integration Active directives from are appended to the skill's system prompt, so user guidance applies even inside skill execution. Installing skills From a local file or directory When pointing to a directory, Agav copies the entire tree (excluding , , , and ). When pointing to a bare SKILL.md file, it copies the file plus sibling asset directories (, , ) if present. Batch install: If the target directory contains no at the root but has subdirectories with files, all subdirectories are installed independently. Failures in one skill don't block others. Size limits: Installations are capped at 200 files and 10 MB total. Exceeding either limit is refused before any files are copied. From a URL Agav normalizes common GitHub URL formats automatically: - → raw content URL - → raw content URL with appended - Bare repo URL → appends If the URL returns an HTML page instead of Markdown, Agav reports a clear error rather than a confusing validation failure. GitHub batch install: If a single URL 404s, Agav checks whether the URL points to a directory containing multiple skill subdirectories and installs all of them. From the marketplace The marketplace opens an interactive TUI: - ↑↓ to navigate, ←→ to page (5 skills per page) - s to search by name or description - ENTER to install the selected skill - r to refresh the catalog - ESC to exit Already-installed skills show a badge and cannot be reinstalled. Removing skills Restart Agav after installing or removing a skill. Creating a skill A skill is a directory containing a file — YAML front-matter followed by instruction body: Frontmatter fields | Field | Type | Required | Description | |-------|------|----------|-------------| | | string | yes | Unique identifier. Must be lowercase with single hyphens (), max 64 chars. | | | string | yes | One-line description, max 1024 chars. | | | string | yes | Semver version string. | | | \\| \\| | no | How the skill can be triggered. Default: . | | | string[] | no | Whitelist of tools the skill can use. Supports parenthesized qualifiers like . | | | string[] | no | Blacklist of tools removed from the skill's registry. | | | string | no | LLM model override for this skill. | | | string | no | Reasoning effort override. | | | string[] | no | Keywords for search and automatic trigger scoring. | | | string | no | License name or file reference. | | | string | no | Environment requirements (max 500 chars). | | | Record | no | Arbitrary key-value pairs. Agav reads its own extension fields from here as a fallback, so skills authored for other platforms that nest extensions under work the same. | Tool name compatibility The and fields accept both Agav tool names and agentskills.io spec names. Spec names are automatically mapped at runtime: | Spec name | Agav name | |-----------|-----------| | | | | | | | | | | , | | | | | | | | | | | | | | | | | | | | | | | | , | | Shell blocks Skill bodies can contain fenced shell blocks (``deny-writesaskauto-acceptcmd.exe /c/bin/sh -callowed-toolsdisallowed-toolssubagentactivateskillnamedescriptionversionnameignore all previous instructionscurl ... | basheval()rm -rf /chmod 777sudobase64 | bashallowed-toolsdisallowed-tools.agav/traces.jsonl/skills info ` shows total runs, average tokens per run, and success rate - Trigger phrases — auto-generated positive and negative triggers from the skill's name, description, and tags - Trigger scoring — user queries are scored against triggers to decide automatic activation (multi-word matches score higher, negative triggers reduce the score) - Optimization notes — after 3+ runs, Agav generates suggestions (e.g., \"tighten instructions to reduce token usage\") - Background improvement — every 15 turns, triggers, runtime prompts, and optimization notes are refreshed for all skills Related - Author and Evolve a Skill — full walkthrough - Compose Agents, Skills, and Tools — using skills with agents",
    "slug": "features/skills"
  },
  {
    "id": "getting-started",
    "title": "Getting Started",
    "content": "Getting Started This path takes you from installation to a safe first change in an existing repository. Before you begin You need: - a terminal - a local Git repository to explore - an API key for OpenAI, Anthropic, Gemini, OpenRouter, or NVIDIA NIM; a Google Cloud service-account JSON file for Vertex AI; or a running Ollama installation The prebuilt Agav release does not require Node.js. Node.js and pnpm are only needed when developing Agav itself. Follow this path 1. Install Agav and verify that the command works. 2. Connect a provider so Agav can use a model. 3. Complete your first task in a real repository, beginning in read-only mode. 4. Configure your project with shared instructions and defaults when you are ready. At the end, you will know how to open the correct workspace, give Agav exact context, review a proposed change, and check what changed. If you are already set up, go directly to the first-task walkthrough.",
    "slug": "getting-started"
  },
  {
    "id": "getting-started/installation",
    "title": "Install Agav",
    "content": "Install Agav Use the prebuilt release unless you are developing Agav itself. macOS or Linux Windows PowerShell Windows Command Prompt The installer adds to your automatically. Open a new terminal, then verify the installation: If is still not found, your shell probably has not picked up the updated yet. First open a fresh terminal window or start a new shell. If that still does not work, add the install directory to manually: - macOS or Linux default install directory: - Windows default install directory: Update Agav To install a specific release, pass the version explicitly: Set to disable automatic update checks. Develop Agav from source This route requires Node.js 22 or newer, pnpm 9 or newer, and Git: Use the repository's rather than mixing package managers. Run if you want this checkout to provide the global command. Next: connect a model provider.",
    "slug": "getting-started/installation"
  },
  {
    "id": "getting-started/projects",
    "title": "Configure a Project",
    "content": "Configure a Project You can use Agav without project configuration. Add it when a repository has conventions that Agav should follow consistently. Start at the project root Agav treats the current directory as its workspace. Start from the repository root so file suggestions and tools can reach the whole project: paths are relative to this directory and cannot escape it through or symlinks. Add repository instructions Create or at the project root. Agav uses the first non-empty file it finds. Write short, actionable rules that apply across tasks. Put the details of a one-time task in its prompt instead. Save project defaults Project settings live in and override global defaults from . Agav creates or enriches the project file without overwriting values already there. Do not commit API keys. Use provider environment variables or secure user-level configuration for credentials. See the configuration reference for every field and the full precedence rules. Know what is shared | Item | Scope | | --- | --- | | or | Repository instructions | | | Project defaults | | | Defaults for your user account | | | Reusable procedures for this project | | and | User-level extensions | | | Saved interactive sessions | You have completed the beginner path. Continue with daily workflows or use the task finder.",
    "slug": "getting-started/projects"
  },
  {
    "id": "getting-started/providers",
    "title": "Connect a Provider",
    "content": "Connect a Provider Agav needs one model provider. The easiest path is: 1. choose one provider 2. set its credential in the same terminal where you will start Agav 3. launch Agav with that provider and model 4. ask one read-only question to confirm it works If you are not sure which provider to pick, use OpenAI, Anthropic, or Gemini if you already have an API key. Use OpenRouter if you want access to multiple providers behind a single key. Use NVIDIA NIM for NVIDIA-hosted models. Use Ollama if you want to run locally. Use Vertex AI if you already run on Google Cloud and want Gemini or Claude billed through that project. Fastest path If you already have an OpenAI key, this is the quickest first run: Then ask: OpenAI OpenRouter OpenRouter aggregates multiple model providers behind a single API key. Set the key as an environment variable: Start Agav with OpenRouter: The default model is , which lets OpenRouter choose the best model for each request. You can also specify any model available on OpenRouter: Or set it in : switches to a lightweight model and to the most capable one available through OpenRouter. NVIDIA NIM NVIDIA NIM provides access to NVIDIA-hosted models through an OpenAI-compatible API. Set the API key: Start Agav with NVIDIA NIM: The default model is . All models use the prefix: Or set it in : Context window sizes are detected automatically from the NVIDIA API. The API base URL is . Anthropic Google Gemini Vertex AI Vertex AI authenticates with a Google Cloud service-account JSON file rather than an API key. Point Agav at the file, then select the provider: Setting the credentials path is what enables the provider; there is no separate on/off flag to keep in sync with it. Agav uses the multi-region endpoint by default — set (for example ) to pin a region instead, which some Claude partner models require. Claude partner models are supported by the same provider and credentials, addressed with the same prefix. Claude models need the versioned ID that Vertex AI exposes, including its suffix: The service account's , , , and optional are read from the JSON file. Agav exchanges the signed credentials for a short-lived OAuth token and refreshes it automatically. Protect the key file. The service-account JSON holds an unencrypted private key that can act as that service account against your entire Google Cloud project. Unlike the API keys Agav encrypts into , this file is yours to secure: keep it outside the repository, it, and grant the service account only the role it actually needs. Ollama Start Ollama and make sure at least one model is installed: Agav lists the models available at the default endpoint. You can also select one directly: For a remote Ollama server, set and, when required, . If you prefer host and port separately, Agav also supports and . Make it stick If you do not want to pass and every time, save defaults in your user config file: - macOS or Linux: - Windows: Example: Keep cloud API keys in environment variables when possible. Agav can also read provider keys from config, but environment variables are safer for shared machines and less likely to end up in version control. Verify the connection Start a read-only session in any repository: Replace the provider and model with your choice. Then ask: If Agav reports a missing key, confirm that the matching environment variable is set in the same terminal where you launched Agav. An explicitly selected provider will not silently use a different provider. If the key is set but Agav still cannot connect: - open a fresh terminal and set the variable again - confirm the variable name matches the provider exactly - for OpenRouter, confirm is set and starts with - for NVIDIA NIM, confirm is set and starts with - for Ollama, confirm the server is running and reachable - for remote Ollama, confirm , or plus - for Vertex AI, confirm points at a readable service-account JSON file, and check if a Claude model requires a specific region You can switch providers or models later with . To save defaults, see configuration. Keep cloud API keys out of project configuration and version control. Next: complete your first repository task.",
    "slug": "getting-started/providers"
  },
  {
    "id": "getting-started/quick-start",
    "title": "Your First Repository Task",
    "content": "Your First Repository Task This walkthrough uses quora/model-deprecation-tracker, a small Python project with a clear entry point and generated outputs. You can use the same prompts in your own repository. 1. Open the repository root Start Agav from the directory that contains the whole project. Agav treats that folder as the project root, so it uses it to decide which files it can see, which project settings to load, and which suggestions to offer. 2. Start read-only mode lets Agav inspect the repository while blocking edits. Replace the provider and model if you configured a different one. 3. Ask a focused first question Paste this prompt: Typing opens file suggestions. Choose a path with Up or Down, then press Enter or Tab to attach it. An attachment gives Agav exact context; the question tells it what to do with that context. For this repository, the answer should identify as the entry point and explain how the scrapers feed the generated data, README, calendar, and optional Slack notifications. 4. Narrow the investigation Follow the broad tour with one concrete path through the code: Focused follow-ups are easier to verify than repeatedly asking for a complete repository summary. 5. Make one controlled change Exit Agav, then restart without : Ask for a small change with a clear boundary: The default mode pauses before sensitive actions and shows edit diffs. Check the file path and proposed change before approving. Reject the action and clarify your request if it is broader than expected. 6. Review the result After Agav finishes, review the repository yourself: These commands distinguish Agav's work from changes that already existed. Use before exiting Agav to restore a tracked file edit, or use your normal Git workflow afterward. What you learned You now know the core Agav loop: 1. open the repository root 2. begin read-only when learning unfamiliar code 3. attach exact context and ask a specific question 4. narrow the scope before requesting a change 5. review confirmations, diffs, and Git state Next: add project instructions and defaults, or learn more about files and context.",
    "slug": "getting-started/quick-start"
  },
  {
    "id": "guides/agent-skill-tool-combinations",
    "title": "Compose Agents, Skills, and Tools",
    "content": "Compose Agents, Skills, and Tools Combine capabilities when one task has reusable rules, quick local checks, and independent investigations. Scenario: review a Bedrock scraper change A change to can affect the shared data shape, generated README table, calendar, and Slack message. This division keeps responsibilities clear: | Layer | Responsibility | | --- | --- | | Skill | Repeatable scraper-review procedure | | Main prompt | This change, its paths, and the required report | | Direct tools | Small checks needed in the main conversation | | Subagents | Independent downstream investigations | Ask the main agent to reconcile the results before starting an implementation phase. Keep temporary repository paths in the prompt, not in a global skill. Expected result You get one review that covers the scraper contract and every downstream consumer without making each worker inspect the whole repository. Next: Automate with Loops, Watch, and Schedules.",
    "slug": "guides/agent-skill-tool-combinations"
  },
  {
    "id": "guides/build-agent-from-template",
    "title": "Create an Agent with the Wizard",
    "content": "Create an Agent with the Wizard The agent creation wizard lets you build a custom agent in four guided steps — name it, generate a system prompt with your LLM, attach MCP servers, and save. Agav writes the manifest for you and registers the agent immediately. This guide walks through creating a agent from scratch, then covers editing existing agents and working with templates. Prerequisites - Agav installed and connected to an LLM provider (needed for automatic system prompt generation) Step 1: Open the Create tab Start Agav and type: Press to switch to the Create tab. This is the \"My Agents\" hub — it shows your user-created agents, saved templates, and a button at the top. If you haven't created any agents yet, the list will be empty except for the entry. Step 2: Start the wizard Navigate to and press to launch the four-step creation wizard. A progress bar at the top tracks your position through the flow: You can press or at any step to go back to the previous one without losing your input. Step 3: Name and describe the agent The first step asks for two fields: Name and Description. Name — type a short, unique identifier for your agent. Names must be lowercase alphanumeric characters, hyphens, dots, or underscores, with a maximum length of 64 characters. For example: Press or to move to the Description field. Description — write a one-line summary of what the agent does. This appears in the agent list and helps the LLM understand when to route queries to this agent: Press to advance to the next step. Step 4: Review the system prompt Agav sends your name and description to your configured LLM provider and auto-generates a system prompt tailored to the agent's purpose. The prompt streams in real-time inside a bordered preview box. Once generation completes, review the prompt. It typically includes: - A role definition (\"You are a GitHub assistant…\") - Guidelines for tool usage - Response formatting instructions If the prompt doesn't match what you need, press to regenerate it. You can regenerate as many times as you like — each attempt produces a fresh prompt based on your name and description. When you're satisfied, press to accept the prompt and continue. Step 5: Select MCP servers (optional) This step shows a list of MCP servers defined in your workspace . Each server appears with a checkbox. - Press / to navigate the list - Press to toggle a server on or off Selected servers are bundled into the agent's manifest, making them available whenever the agent runs. This is useful for agents that need access to specific tools — for example, a agent might use a GitHub MCP server for repository operations. If you don't need any MCP servers, or none are configured in your workspace, press to skip this step and continue. Step 6: Review and save The final step shows a summary of everything you've configured: - Name — - Description — \"Helps with GitHub repository management tasks\" - Destination path — where will be written - System prompt preview — the generated prompt text - MCP servers — any servers you selected - Tags — auto-generated from your name and description Review the summary. If anything looks wrong, press to go back and make changes. Press to create the agent. Agav: 1. Writes the manifest to disk 2. Registers the agent in the agent registry 3. Validates the manifest 4. Reloads the agent list You'll see a confirmation message once the agent is ready. It appears immediately in both the List and Create tabs. Editing an existing agent To modify a user-created agent, open the Create tab, navigate to it in the list, and press . The wizard opens with all fields pre-populated from the existing manifest. During edits: - Name is read-only — you cannot rename an agent after creation - Description can be updated freely - System prompt can be regenerated with or accepted as-is - MCP servers can be added or removed The same four-step flow applies. Press on the final review step to save your changes. Working with templates Templates preserve deleted agents so you can restore them later. How templates are created — when you delete a user-created agent (from either the List or Create tab), Agav automatically saves it as a template. Nothing extra is required. Finding templates — templates appear in the Create tab alongside your active agents, marked with a label. Restoring a template — navigate to a template and press . The wizard opens with all fields pre-populated (name, description, system prompt, and MCP servers). Walk through the steps and press on the review step to recreate the agent. Deleting a template permanently — navigate to a template and press . This removes it from the template store with no way to recover it. Storage — templates are stored in . This file is managed automatically; you don't need to edit it by hand. Keyboard reference | Key | Action | |-----|--------| | / | Navigate list or switch wizard fields | | | Switch between name and description fields | | | Select / advance / save | | | Toggle MCP server selection (step 5) | | | Regenerate system prompt (step 4) | | / | Go back one step or exit wizard | | | Delete agent or template from list | Related - Agents — how agents work - Build a Native Agent — manual agent creation with custom tools - Agent Manifest — format reference - Install and Configure an Agent — marketplace agents",
    "slug": "guides/build-agent-from-template"
  },
  {
    "id": "guides/build-native-agent",
    "title": "Build a Native Agent",
    "content": "Build a Native Agent A native agent is a directory containing an manifest and one or more tool files. Agav loads these at startup from three locations (bundled, global, project) and registers them as callable sub-agents. File structure AGENT.md The manifest uses YAML front-matter followed by the agent's system prompt: Key front-matter fields: | Field | Notes | | --- | --- | | | Must be unique across all loaded agents. Used as the tool name: . | | | List of env var names. Agav will prompt for these in the config editor and encrypt them. | | | Map of tool name → or . tools require confirmation before running. | | | Set to to ship disabled by default (users opt in). | | / | Optional per-agent overrides; omit to inherit from the session. | See Agent Manifest reference for all fields. Writing a tool Each file exports a single default object with and : Rules: - must match the key used in in AGENT.md. - corresponds to in AGENT.md permissions. Set it on tools that write, edit, or delete data. - Credentials come from — Agav injects them at runtime, scoped to the agent. - Return — always a plain string, formatted for human reading. - Use only and Node.js built-ins (, , etc.). No npm packages. Installing locally After installing, open and press on the agent to enter credentials. Using the LLM to implement tools If you create stub tools with in their descriptions, Agav can implement them for you. In the List tab: 1. Select the agent and press to inspect 2. Press — \"Implement tools\" 3. Agav sends each stub to the LLM with the agent's context and writes back a full implementation This works for global agents whose tools contain in their description. Bundled and marketplace agents are excluded. Testing Enable the agent and ask Agav a relevant question: Then in the main chat: The LLM routes the query to and invokes . The first call to any tool will show a confirmation prompt. Sharing via the marketplace To publish your agent to the community marketplace: 1. Fork or contribute to the agav marketplace repository 2. Add your agent directory under 3. Add an entry to --- A2A Agents — Experimental > Note: A2A (Agent-to-Agent) support is implemented but not production-tested. Use for experimentation only. An A2A agent runs as an external process (any language) and communicates over HTTP. Agav manages the process lifecycle and proxies calls to it. Manifest Protocol The external process must implement three endpoints: | Endpoint | Method | Behaviour | | --- | --- | --- | | | GET | Return HTTP 200 when ready | | | POST | Accept , return | | | POST | Accept , respond with SSE events: , , , , | Agav polls for up to 10 seconds on startup before routing the first request. Limitations compared to native agents: A2A agents do not currently support credential injection, HITL confirmation propagation, or per-agent MCP servers. These features are available for native agents only. Related - Agent Manifest reference — all AGENT.md fields - Install and Configure an Agent — end-user walkthrough - Agents — how agents are loaded and routed",
    "slug": "guides/build-native-agent"
  },
  {
    "id": "guides/create-search-skill",
    "title": "Author and Evolve a Skill",
    "content": "Author and Evolve a Skill Use a skill when you repeat the same procedure across several tasks. Scenario: review provider scrapers consistently Create in the tracker repository: Restart Agav, then run the skill manually: keeps the workflow predictable: it runs only when you call it. The tool allowlist also keeps the review read-only. Improve it from real use Inspect its configuration and usage: After several runs, change one instruction when you see a repeated problem—for example, add a replacement-model check if reviews keep missing it. Keep project skills in source control and review instruction changes like code. Expected result Every provider review follows the same checks and produces the same compact report format. Next: Compose Agents, Skills, and Tools.",
    "slug": "guides/create-search-skill"
  },
  {
    "id": "guides",
    "title": "Guides",
    "content": "Guides Guides are end-to-end recipes for workflows that combine several Agav capabilities. They assume you have completed Your First Repository Task. For a single command or feature, use Workflows, Features, or Reference instead. Keeping those explanations in one place prevents the same setup and command details from drifting across multiple pages. The examples use quora/model-deprecation-tracker, but the patterns apply to other repositories. Choose a guide | When you need to… | Guide | | --- | --- | | Reduce elapsed time for independent reads and checks | Run Tools in Parallel | | Delegate independent investigations | Coordinate Parallel Subagents | | Build and improve a repeatable procedure | Author and Evolve a Skill | | Combine reusable policy, direct tools, and delegation | Compose Agents, Skills, and Tools | | Repeat work on an interval, file change, or schedule | Automate with Loops, Watch, and Schedules | | Carry durable knowledge across conversations | Use Memory Across Sessions | | Connect an external tool or data source | Connect an MCP Server | | Create a custom agent with the wizard | Create an Agent with the Wizard | | Add a local JavaScript tool | Create a Local Plugin | | Operate a multi-phase task with checkpoints | Operate a Long-Running Task | Start with the narrowest guide that matches your task. Add subagents, automation, or extension points only when the work has a clear independent boundary or will be repeated.",
    "slug": "guides"
  },
  {
    "id": "guides/install-and-configure-agent",
    "title": "Install and Configure an Agent",
    "content": "Install and Configure an Agent This guide walks through installing the Jira agent from the marketplace, configuring its credentials, and using it in a session. The same steps apply to any marketplace agent. Prerequisites - Agav installed and connected to an LLM provider - Credentials for the service you're connecting (e.g. a Jira API token) Step 1: Open the marketplace Start Agav and type: Press to switch to the Marketplace tab. Agav fetches the catalog. You'll see agents listed three per page with name, description, tool count, and category. Step 2: Find and inspect the agent Use / to navigate and / to page through results. Press to search by name or keyword. Navigate to jira and press to inspect it. The inspect view shows the full description, tool list (with and labels), and required credentials. Press or to go back. Step 3: Install With the agent selected, press . A destination prompt appears: Press to install globally. Agav copies the agent files and registers it. You'll see once complete. Step 4: Switch to the List tab Press to open the List tab. You should see the jira agent under Global: with status . Step 5: Inspect the agent Navigate to the jira entry and press . The inspect view shows: - Required Config — credentials the agent needs - Tools — individual tool names with / labels - Model / Effort — (can be overridden) Step 6: Open the config editor Press to open the credential and settings editor. Each required credential appears in the list with its status: Navigate with / and press on a field to edit it. Step 7: Enter credentials For JIRAURL: type or paste your Jira base URL (e.g. ), then press . For JIRAEMAIL: enter your Atlassian account email, then . For JIRAAPITOKEN: paste your Atlassian API token — paste with / , then . After saving each value, the status changes to . Press to return to the inspect view. Step 8: Enable the agent Press to return to the List tab. Navigate to jira and press to toggle it enabled. The status changes to . Step 9: Use the agent Return to your main chat and ask Agav something Jira-related: Agav routes the query to the jira agent. The first time it calls a tool (like creating or updating an issue), you'll see a confirmation prompt: Read-only () tools like run without prompting. Troubleshooting after entering credentials — credentials are checked immediately on save. If the indicator persists, press again and verify the values, particularly that there are no leading or trailing spaces. Agent not appearing in the list — press in the marketplace to refresh, or restart Agav if you just installed. Credential forgotten — open the config editor again and re-enter the value. Old values are overwritten. Related - Agent Marketplace — browsing and installing - Agents — how credentials and permissions work - Build a Native Agent — write your own agent",
    "slug": "guides/install-and-configure-agent"
  },
  {
    "id": "guides/leverage-memory",
    "title": "Use Memory Across Sessions",
    "content": "Use Memory Across Sessions Use memory for verified facts that should influence future conversations, not for temporary task progress. Supported memory commands | Command | Intent | | --- | --- | | | Save as a durable project memory. | | | List saved memories for the current project. | | | Save as a project memory. | | | Show where the current project's memories are stored. | | | Remove the saved memory named . | | | List memories so you can select one to remove. | | | Remove the saved memory named . | | | Remove every memory for the current project. | Scenario: preserve the generated-output contract | Command | Intent | | --- | --- | | | Save the verified generated-output contract as a durable project memory. | | | Confirm that Agav saved the generated-output contract. | | | Find the project's memory directory when you need to inspect it. | The tracker writes the same entries to three outputs. Save that stable convention: Inspect the saved entry: Exit Agav, reopen it from the same Git repository, and ask: Agav should recall the convention because memory is scoped from the Git repository root. Remove stale knowledge | Command | Intent | | --- | --- | | | List memories before choosing the outdated entry. | | | Remove the specific outdated memory named . | | | Remove all project memories when none should be retained. | If the project stops generating one of those outputs, list the memories and remove the outdated entry: Replace with the memory name shown in the list; for example, . Use only when every project memory should be removed. Do not save current plan steps, branch names, guesses, or one-off errors. Expected result Future tracker sessions begin with the verified output contract without carrying temporary work forward. Next: Sessions and Memory.",
    "slug": "guides/leverage-memory"
  },
  {
    "id": "guides/long-running-agent",
    "title": "Operate a Long-Running Task",
    "content": "Operate a Long-Running Task Long tasks stay manageable when discovery, implementation, and review have explicit boundaries. Scenario: add the Azure Foundry deprecation scraper Give Agav the outcome, source of truth, constraints, and checkpoints. This keeps the web research, repository edits, tests, and generated documentation in one auditable workflow: The first phase deliberately requires the official page before implementation: this makes the scraper traceable to a source that can be rechecked when Azure changes its policy. Add a task-specific skill For a task that will recur, add a project skill before starting. Its clear description lets Agav automatically select it when a prompt mentions Azure Foundry deprecations, a scraper, or generated README data; you can also invoke it explicitly. Create : Restart Agav after creating the skill. Use the same task through the explicit skill command when you want to guarantee the procedure is applied: Add a deterministic README check plugin Use a local plugin for the mechanical generated-output check rather than relying on the model to visually compare a long README. Create : Restart Agav and verify that lists . After the scraper's generator completes, prompt Agav: Check progress with . If a requirement changes, steer future work without discarding completed investigation: Use subagents only for independent research or downstream reviews—for example, one can compare the official Azure Foundry page with the proposed field mapping while another reviews the README generator. Keep the main agent responsible for the shared contract, edits, tests, and final diff. Before implementation, review the proposed mapping. Before completion, review changed paths, generated outputs, tool failures, and unresolved assumptions. Use if the conversation becomes long; save memory only for a durable decision that should affect later sessions. Expected result The Azure Foundry addition progresses through visible phases, pauses before edits, and ends with a source-backed scraper, focused tests, and a deterministic check that README generation includes the new entries. Next: Planning and Steering, Author and Evolve a Skill, and Create a Local Plugin.",
    "slug": "guides/long-running-agent"
  },
  {
    "id": "guides/mcp",
    "title": "Research GitHub with an MCP Server",
    "content": "Research GitHub with an MCP Server This guide connects Agav to GitHub's official MCP server, then uses its read-only tools to investigate an issue before changing local code. It is a practical companion to Model Context Protocol, which explains Agav's MCP support without repeating this setup. Agav commands used in this guide | Command | Intent | | --- | --- | | | Confirm that Agav started the GitHub server and discovered its MCP capabilities. | 1. Prepare a read-only GitHub connection GitHub's local MCP server runs in Docker. Install and start Docker, then create a GitHub personal access token with only the repository access needed for the repositories you will inspect. Keep the token out of project configuration and version control. In the terminal that will start Agav, export the token and restrict the server to repository and issue tools: The read-only switch prevents the GitHub MCP server from exposing write tools, even when its enabled toolsets include them. See GitHub's local server prerequisites and token guidance before choosing token permissions. Add the following server definition to . This file is project-specific; use instead when you want the connection available in every project. The entries pass the exported values into Docker; they do not put the token in the configuration file. Agav merges the global configuration first and then the project configuration, so a project server with the same name overrides its global definition. Restart Agav after changing MCP configuration. 2. Verify the server Run . Under MCP servers, confirm that is listed. If it is not, first run the Docker command from the configuration directly in your terminal to check that Docker is running and that the exported token is available. MCP startup failures do not prevent Agav from starting, so is the quickest way to distinguish an unavailable server from a normal Agav session. 3. Use GitHub tools for a repository task Ask Agav to use the GitHub tools explicitly and to keep all GitHub activity read-only. For example, before fixing a local scraper failure: Agav can use the discovered issue and repository tools to gather the upstream context, then return a local implementation plan. Follow up with a scoped request such as: This separates external research from local changes: GitHub remains read-only, while Agav's normal permission mode still controls edits and shell commands in the checkout. Troubleshooting and scope | Symptom | What to check | | --- | --- | | is absent from | Docker is running, the image can be pulled, and is exported in the same terminal session that starts Agav. | | GitHub requests are unauthorized | Recreate or adjust the token with the minimum access required for the target repository, then restart Agav. | | Too many unrelated tools appear | Keep limited to the task, such as ; GitHub documents the available toolsets and individual-tool options. | | You need to create or update GitHub content | Remove read-only mode only after reviewing the required token permissions and the task's intended write scope. | Agav supports both stdio subprocess and remote HTTP/SSE MCP transports. GitHub's local MCP server uses the stdio transport shown above. For servers that expose an HTTP or SSE endpoint instead, see Model Context Protocol — Remote servers. For server options such as toolsets and read-only mode, see the GitHub MCP server documentation.",
    "slug": "guides/mcp"
  },
  {
    "id": "guides/parallel-agents",
    "title": "Coordinate Parallel Subagents",
    "content": "Coordinate Parallel Subagents Use subagents when independent investigations need more context than a few tool calls. Scenario: audit every provider scraper The tracker has separate implementations for OpenAI, Anthropic, Google, and AWS. They can be reviewed independently, then compared against the shared contract. Each assignment includes its files, checks, output, and no-edit constraint because subagents work independently and cannot ask follow-up questions. Review the combined result Use to inspect active subagents. Sensitive confirmations still appear in the main terminal. When all workers finish, ask the main agent to resolve disagreements and remove duplicate findings. Delegate edits only when workers own different files; worktrees reduce interference but do not make overlapping changes safe. Expected result You receive three bounded audits and one reconciled list of scraper risks, while the main conversation retains final ownership.",
    "slug": "guides/parallel-agents"
  },
  {
    "id": "guides/parallel-tools",
    "title": "Run Tools in Parallel",
    "content": "Run Tools in Parallel Use parallel tools for short, independent checks that belong in the same conversation. Scenario: map the tracker before changing a scraper Before editing a provider scraper, you need to understand three separate parts of the project: - where scrapers are registered - the shape stored in - how calendar events are generated Ask Agav to inspect them together: Agav can run independent reads and searches concurrently, then return the evidence to the same conversation. Keep dependencies in order Do not parallelize steps when one changes what the next step reads. State the order explicitly: Keep overlapping edits and state-changing commands sequential. Use parallel subagents when each investigation is large enough to need its own context. Expected result You get one concise map of the scraper registry, shared data shape, and calendar output without waiting for each independent read in sequence. Next: Coordinate Parallel Subagents.",
    "slug": "guides/parallel-tools"
  },
  {
    "id": "guides/plugins",
    "title": "Create a Local Plugin",
    "content": "Create a Local Plugin Plugins are useful when Agav needs a deterministic operation that is not built in. They run with the same operating-system access as Agav, so use only code you trust. Scenario: report the next model shutdown Create : Restart Agav, check for , then ask: The tool expects Agav to be running at the tracker root. Validate inputs and handle file errors before expanding a plugin to accept user-supplied paths or perform writes. Expected result Agav gets a short, deterministic answer from the repository's current JSON data without asking the model to sort the full file. Next: Plugins and Subagents.",
    "slug": "guides/plugins"
  },
  {
    "id": "guides/scheduler",
    "title": "Automate with Loops, Watch, and Schedules",
    "content": "Automate with Loops, Watch, and Schedules Agav provides three session-based automation mechanisms. Agav must remain open for all three. Scenario: monitor tracker work Repeat an agent check While investigating generated data, ask Agav to recheck it every 15 minutes: Stop it with . React to scraper edits The repository already has checks. Run the command once yourself, then watch the scraper directory: Stop it with . Watch commands run directly in your shell, so use only commands you trust. Save a timed review Create a weekday morning prompt: Use , then enable, disable, or remove a task by ID. Choose the right mechanism | Need | Use | | --- | --- | | Repeat an agent prompt during this session | | | Run a command after a file changes | | | Save a cron prompt that runs while Agav is open | | | Run after logout or in CI | An external scheduler with or | Expected result Tracker reviews repeat at the right trigger without treating Agav as an unattended system service. Next: Use Memory Across Sessions.",
    "slug": "guides/scheduler"
  },
  {
    "id": "index",
    "title": "Agav Documentation",
    "content": "Agav Documentation Agav is a terminal-native AI coding assistant for real repositories. It can inspect code, edit files, run commands, and help verify changes from a CLI-first workflow. New to Agav? Follow the four-page path below. You do not need to understand agents, tools, skills, or model settings before you begin. Get started 1. Install Agav 2. Connect a model provider 3. Complete your first repository task 4. Add instructions for your project The Getting Started overview lists what you need and what you will learn at each step. Find what you need | I want to… | Go to | | --- | --- | | Give Agav a specific file, document, or image | Files and context | | Continue a previous conversation | Sessions and memory | | Structure or redirect a larger task | Planning and steering | | Run Agav from a script or CI job | Non-interactive and CI | | Automate repeated work | Automation | | Look up a command or setting | Reference | | Diagnose a problem | Troubleshooting | Learn advanced workflows After you are comfortable with the basics, use the guides for complete recipes involving subagents, skills, MCP, plugins, and long-running work. Use the features section when you want to understand an individual capability. > Workspace boundary: Agav treats the directory where you start it as the active workspace. Start at your repository root so it can see the whole project.",
    "slug": "index"
  },
  {
    "id": "reference/agent-manifest",
    "title": "Agent Manifest (AGENT.md)",
    "content": "Agent Manifest (AGENT.md) Every agent is defined by an file: a YAML front-matter block followed by the system prompt in Markdown. Agav parses the front-matter at load time and passes the Markdown body as the agent's system prompt. Structure Front-matter fields Core fields | Field | Type | Required | Default | Description | | --- | --- | --- | --- | --- | | | string | yes | — | Unique identifier. Used as the tool name: . Must be lowercase with hyphens only. | | | string | yes | — | One-line description shown in the agent catalog and marketplace search. Guides the LLM when routing queries. | | | string | yes | — | Semver version string (e.g. ). | | | \\| | no | | Execution model. runs JS tools in-process; delegates to an external HTTP process. | | | boolean | no | | Whether the agent is active at startup. Bundled agents ship with — users opt in after configuring credentials. | Configuration | Field | Type | Required | Default | Description | | --- | --- | --- | --- | --- | | | string[] | no | — | Environment variable names the agent needs. Agav collects these in the config editor and stores them encrypted in . Injected into during the agent's execution only. | | | string | no | | Relative path to the directory containing tool files. | Model and effort | Field | Type | Required | Default | Description | | --- | --- | --- | --- | --- | | | string | no | inherit session | LLM model override for this agent. Shown as in the inspect view; clear the value in the config editor to revert to inheriting. | | | \\| \\| \\| | no | inherit session | Reasoning effort override for this agent. | Discovery | Field | Type | Required | Default | Description | | --- | --- | --- | --- | --- | | | string[] | no | — | Keywords shown in the marketplace and used for search filtering. | | | string | no | — | Attribution displayed in the inspect view. | Tool permissions | Field | Type | Required | Default | Description | | --- | --- | --- | --- | --- | | | Record\\ | no | — | Per-tool HITL classification. runs without confirmation. pauses and shows before executing. Keys must match the values in the tool files. | Per-agent MCP servers MCP servers declared here are started when the agent runs and stopped when it finishes. They are scoped to the agent — other agents and the parent session cannot access them. provides per-server environment variables that are layered on top of the process environment and agent credentials from . A2A fields (experimental) Used when . See Build a Native Agent — A2A section for the protocol specification. | Field | Type | Required | Description | | --- | --- | --- | --- | | | string | yes (A2A) | Shell command to launch the external process. | | | string | yes (A2A) | HTTP base URL of the running process (e.g. ). | Tool file format Each file in exports a default object: must match the classification: → , → . System prompt body The Markdown content after the closing delimiter becomes the agent's system prompt. It is prepended to the base Agav system prompt at runtime. Keep it focused: - One role sentence: \"You are a \\ assistant...\" - 3–5 guideline bullets covering precision, confirmation for modifying tools, output formatting, and error handling - Avoid restating tool schemas — that information is already in the tool definitions Related - Agents — loading, routing, and credentials - Build a Native Agent — full walkthrough with examples - Agent Marketplace — installing from the catalog",
    "slug": "reference/agent-manifest"
  },
  {
    "id": "reference/changelog",
    "title": "Changelog",
    "content": "Changelog Notable changes in each Agav release, newest first. v0.2.0 All beta features promoted to stable. This is the first non-pre-release since v0.1.9. - Promotes the full v0.2.0-beta feature set (skills, mouse support, agent wizard, sandbox hardening, new providers) to stable - Fixes the pre-release → stable upgrade path in auto-update v0.2.0-beta.3 Adds a productivity skill suite and fixes several session-stability issues. - Adds bundled skills for documents, meetings, writing, data analysis, and authoring - Fixes UI lag in long-running and resumed sessions - Fixes a mouse-sequence leak that could corrupt terminal output - now exits cleanly - Adds a vision statement to the README v0.2.0-beta.2 Introduces mouse support, an agent creation wizard, and remote MCP transport. - Vendors Ink with full mouse support — scrolling and click-to-position cursor placement - Adds an agent creation wizard with a My Agents hub, workspace MCP selection, and template persistence - Adds HTTP/SSE remote transport for MCP servers - Launches the documentation website - Fixes table display rendering and input guard edge cases v0.2.0-beta.1 Hardens the sandbox, adds two new providers, and ships a batch of platform fixes. - Closes 9 sandbox gaps across network, filesystem, and permission boundaries - Adds NVIDIA NIM and OpenRouter providers - Adds a thinking-token toggle () and turn duration in the status bar - Renames to with an interactive session picker - Fixes Windows-specific issues (Bun segfault, flag, grep/find ENOENT errors) - Speeds up auto-update with inline SHA-256 hashing v0.1.9 Introduces agents, a marketplace, and session-scoped plans. - Adds an agent system with a public marketplace for discovering and installing agents - Adds session-scoped plans with a plan detail panel - Reduces binary size with gzipped release assets v0.1.8 Hardens installers and auto-update reliability. - Hardens install scripts and auto-update across platforms - Adds installer test suites - Cleans up PATH handling left over from earlier versions v0.1.1 – v0.1.7 Earlier releases focused on core infrastructure, provider support, and tool reliability.",
    "slug": "reference/changelog"
  },
  {
    "id": "reference/cli",
    "title": "CLI Reference",
    "content": "CLI Reference Modes Options | Option | Description | | --- | --- | | , | , , , , , , or | | , | Provider model identifier | | | , , , or | | | Ollama host when no complete endpoint is set | | | Ollama port | | | Complete Ollama base URL | | | Ollama bearer token | | , | Print mode | | | Stream print-mode response text | | | Validate print-mode output against JSON Schema | | | Tool policy for | | | Limit iterations in | | , | Open the session picker or resume by ID prefix | | , | Skip normal tool confirmations | | | OpenAI API mode: (default) or . Use for OpenAI-compatible endpoints that don't support the Responses API. | | | Block write operations | | , | Print the version | | , | Print help | Both and are accepted for provider, model, effort, Ollama values, output schema, permissions, and max turns. Environment variables | Variable | Purpose | | --- | --- | | | Anthropic credential | | | OpenAI credential | | | OpenRouter credential () | | | NVIDIA NIM credential () | | | Gemini credential | | | Path to a Google Cloud service-account JSON file; enables Vertex AI | | | Vertex AI region, or for the multi-region endpoint (default ) | | | Complete Ollama endpoint | | / | Ollama address components | | | Ollama bearer token | | | JSON policy used by | | | Disable automatic update checks | | | Disable automatic shell sandbox selection | | | Override the default agent marketplace URL | | | Office document conversion executable | Agent subcommands | Option | Description | | --- | --- | | | Install under an alternative name (resolves name conflicts) | | | Install scope; defaults to | Local paths and GitHub repository URLs are both supported for . For GitHub, Agav uses sparse checkout to download only the agent directory.",
    "slug": "reference/cli"
  },
  {
    "id": "reference/configuration",
    "title": "Configuration",
    "content": "Configuration Agav merges defaults, , and in that order. Environment variables then override provider credentials and Ollama address settings; CLI flags override the active startup values. Fields | Field | Values or behavior | | --- | --- | | | , , , , , , or | | | Provider-specific model identifier | | | Path to a Google Cloud service-account JSON file used by Vertex AI; setting it enables the provider. Supports a leading . Prefer . | | | Vertex AI region, or for the multi-region endpoint (default ). Can also be set with . | | | , , , or ; invalid values fall back to | | | Maximum output tokens per model response | | | Maximum agent/tool iterations; must be a positive integer | | | Transient provider retries; must be zero or greater | | | , , or | | | When , refuse to start if no OS-level sandbox (Seatbelt, Bubblewrap, or Docker) is available | | | Tool names or scoped patterns that can run without confirmation | | | Additional project instructions | | | Optional commands for , , and | | | Partial terminal color overrides | | | Named MCP server definitions (stdio and remote) | | | URL of the agent marketplace (supports and ). Defaults to the official marketplace. Set to override without editing the config file. | Thinking tokens Some models (such as Claude with extended thinking or certain OpenAI o-series models) support thinking tokens — internal reasoning steps the model performs before producing its final answer. Press during a response to toggle visibility of thinking text. Thinking tokens count toward context usage and may increase cost. The status bar shows thinking token counts when available. MCP server fields Stdio servers (local subprocess): | Field | Type | Description | | --- | --- | --- | | | string | Executable to run (e.g. , , ) | | | string[] | Arguments passed to the command | | | Record | Environment variables for the subprocess | Remote servers (HTTP/SSE): | Field | Type | Description | | --- | --- | --- | | | string | HTTP/SSE endpoint URL | | | | Marks the entry as a remote server; inferred when is present | | | \\| | Force a specific transport; omit to auto-detect (tries HTTP first, falls back to SSE) | | | Record | Extra headers sent with every request (e.g. ) | Project entries are added to global entries rather than replacing them. The generated top-level object in project configuration is documentation metadata and is removed before runtime merging.",
    "slug": "reference/configuration"
  },
  {
    "id": "reference",
    "title": "Reference",
    "content": "Reference - CLI reference - Slash commands - Configuration - Keybindings - Security - Troubleshooting - Changelog lists what changed in each Agav release. Use for the installed release's startup options and for interactive commands, including commands added by skills and MCP prompts.",
    "slug": "reference"
  },
  {
    "id": "reference/keybindings",
    "title": "Keybindings",
    "content": "Keybindings Agav loads defaults, then , then . Project bindings replace global or default bindings for the same action. | Action | Default | | --- | --- | | Cancel | | | Expand tool detail | | | Plan detail panel | | | Toggle thinking text | | | Toggle compaction summary | | | Cycle subagents | | | Insert newline | , or on macOS terminals that support it | | Submit | | | Prompt history | / | | Interrupt | | | Clear input | | | Delete previous word | | | Edit last prompt | | | Retry last turn | | | Command palette | | | Show keybindings | | | Clear screen | | | Exit | | | Scroll up | , | | Scroll down | , | | Scroll to top | | | Scroll to bottom | | Override bindings Values can be a string or an array. Chords separate strokes with spaces: Names are case-insensitive. , , and normalize to , , and . Mouse interaction Agav enables terminal mouse tracking to support scrolling and text selection without leaving the TUI. | Action | Gesture | | --- | --- | | Scroll | Mouse wheel anywhere in the conversation | | Position caret | Click inside the input prompt | | Select text (input prompt) | Click and drag inside the input prompt | | Select text (conversation) | Click and drag anywhere in the conversation area | | Select word | Double-click | | Select line | Triple-click | | Copy to clipboard | Selection is copied automatically on mouse-up | | Extend selection | / (input prompt only) | Selected text in the input prompt is highlighted in cyan. Typing while text is selected replaces the selection. Pressing an arrow key without Shift clears the selection and moves the caret. Conversation-area selections operate on the rendered frame and copy plain text (ANSI codes are stripped). Scrolling or typing clears an active conversation selection. Terminal protocols determine which key combinations Agav can distinguish. Many terminals encode as Enter and do not distinguish ; use / () when your terminal supports it.",
    "slug": "reference/keybindings"
  },
  {
    "id": "reference/security",
    "title": "Security",
    "content": "Security Agav can read files, edit code, and execute commands. Choose controls appropriate to the repository and environment. Permission modes | Mode | Behavior | | --- | --- | | | Confirm sensitive actions and show edit diffs | | | Skip normal confirmations for faster trusted workflows | | | Block writes and mutation-oriented operations | Set a default in configuration or use and at startup. can auto-approve named tools or scoped command patterns. deny-writes mode In mode, all file-mutating tools (, , , ) are blocked unconditionally — even if they appear in the list. This prevents a blanket allowlist grant from quietly authorising destructive operations. Safe tools These tools never require confirmation because they cannot modify the working tree: , , , , , , , , , , , External tool trust External agents and MCP tools cannot mark themselves as non-destructive to skip confirmation. Only built-in safe tools are trusted with the flag. All other tools require confirmation in mode regardless of their declared destructive status. Shell sandbox Agav auto-detects the best available OS-level sandbox at startup: | Platform | Backend | Mechanism | | --- | --- | --- | | macOS | Seatbelt | with a deny-default profile | | Linux | Bubblewrap | with read-only root and network isolation | | Docker | Container | , memory and CPU limits | | Windows | Env-var shaping | Strips proxy vars, sets | If no backend is available, commands run unsandboxed. Set to intentionally disable sandbox detection. The same sandbox backends also protect agent tool execution. When an agent runs a tool file, the tool process inherits the sandbox restrictions of the active backend. Bundled agents are trusted and run unsandboxed; global and project agent tools are sandboxed. Seatbelt (macOS) The Seatbelt profile uses deny-default with targeted allows: - Reads — allowed across the filesystem, except , , and - Writes — allowed only in the working directory and system temp directory - Network — fully denied - Process execution — allowed, except - IPC — Mach lookup, sysctl reads, and POSIX shared memory are allowed for basic process operation Bubblewrap (Linux) - Filesystem — root is mounted read-only (); only the working directory and are writable - Credentials — , , , and are replaced with empty tmpfs mounts - Network — fully isolated () - Lifecycle — child processes are killed when Agav exits () Docker - Network — disabled () - Resources — 512 MB memory, 1 CPU - Filesystem — only the working directory is mounted into the container Windows Windows has no kernel-level sandbox. As a best-effort mitigation, Agav: - Sets so well-behaved child tools can self-restrict - Strips , , (and lowercase variants) to reduce network reach MCP command validation On Windows, MCP server subprocesses use for shim compatibility. Before spawning, Agav validates both the command and all arguments against a set of blocked shell metacharacters: ` $ ( ) { } [ ] ! % \" \\n \\r `'cmd.exeshell: falseKEYSECRETTOKENPASSWORDCREDENTIALAUTHsandboxRequired: true~/.agav/config.json.agav/config.jsonreadfilewritefileeditfile~/.agav//.git//.agav//etc/passwd~/Desktop/file.txt~/.ssh/~/.aws/~/.gnupg/~/.kube/configauto-acceptrm -rf /rm -rf ~rm -rf .git reset --hardgit push --forcegit clean -fgit branch -Dsudo rmsudo dddd if=mkfs./dev/sdchmod -R 777chown -RdropdbDROP DATABASEkillallpkill -9curl ... | shwget ... | sh/bashtruncate --size 0config.jsonVERTEXAICREDENTIALSPATHvertexAICredentialsPathroles/aiplatform.user./.agav/config.json--max-turns` to limit unattended work.",
    "slug": "reference/security"
  },
  {
    "id": "reference/slash-commands",
    "title": "Slash Commands",
    "content": "Slash Commands Type in an interactive session to autocomplete commands. Run to see the commands loaded in the current session, or for detailed usage. Skills and MCP servers can add commands at runtime, so they are not listed individually here. Chat and models | Command | Purpose | | --- | --- | | | List available commands or show detailed usage for one command, including . | | , | Start a clean chat without deleting saved sessions. | | | Open the model picker or switch to a specific model; Agav switches provider when needed. | | | Show or set reasoning effort. | | , | Switch to the current provider's fast or most capable model shortcut. | | | Summarize older conversation content to free context-window space. | | | Show a token-use breakdown for the system prompt, tools, MCP tools, skills, and messages. | | | Write the current conversation, including tool activity, to a timestamped Markdown file in the working directory. | | | Run a brief independent side query with read-only conversation context, without interrupting the active main task. It cannot use tools or plugins. | Sessions and workflow | Command | Purpose | | --- | --- | | | List saved sessions or resume one by ID or prefix match. | | | Search saved session messages by keyword. | | | Give the current session a descriptive name. | | | List branches in the current session lineage, or fork the current conversation into a named branch. | | | Show this session's plan and step progress. | | | List every plan saved for the current project. | | | Set step to , , , or . | | | Delete this session's plan. Use in a prompt to skip automatic planning. | | | Add durable direction for later turns. Use , , or to manage directives. | | | Show the active loop. repeats an agent prompt; intervals support , , and . defaults to ten minutes, and cancels it. | | | List saved tasks. Use , then , , or to manage them. | | | Run a shell command after matching files change. cancels it; glob patterns are supported. | | , | Revert the most recent tracked file change or inspect the current in-memory undo stack. | Memory and skills | Command | Purpose | | --- | --- | | , | List project-scoped persistent memories. | | | Save a persistent memory. | | , | Delete one memory by its listed name. | | , | Delete all project memories or show their storage directory. | | | Shortcut for . | | | List memories when called without an argument, or delete a memory by name. | | , | List bundled, global, and project skills. | | | Show a skill's origin, invocation mode, tool policy, path, and usage statistics. | | | Validate and install a skill from a local or trusted URL. Restart Agav to activate it. | | | List available marketplace skills or install a numbered entry. | | , | Remove an installed global skill. Restart Agav to apply the change. | | | Remove all user-installed (global) skills. Bundled and project skills are unaffected. Restart Agav to apply. | | | Invoke an installed user-callable skill. The exact command name comes from the skill's slug. | Agents | Command | Purpose | | --- | --- | | | Open the agent management TUI. List tab shows installed agents with credentials status, enable/disable toggle, and config editor. Marketplace tab browses, searches, and installs community agents. Create tab lists user-created agents and templates, and opens a 4-step wizard to create or edit agents. | Diagnostics and exit | Command | Purpose | | --- | --- | | | Show conversation, plan, sandbox, token, plugin, MCP, undo, and memory state. | | | Show release notes from the latest update. | | | Save the current session and exit Agav. | MCP prompts appear as additional slash commands supplied by their connected server. Project skills and user-installed skills likewise add commands after Agav restarts; use or type to see what is available in the current session.",
    "slug": "reference/slash-commands"
  },
  {
    "id": "reference/troubleshooting",
    "title": "Troubleshooting",
    "content": "Troubleshooting No API key found Set , , or ; set to a service-account JSON file for Vertex AI; or run Ollama and select it explicitly. If you pass , Agav requires that provider's credential instead of falling back to another configured provider. No Ollama models found Confirm the server is reachable and has a model: Use for remote or hosted installations. A file mention is rejected paths must stay inside the directory where Agav started. Start Agav higher in the repository, correct the relative path, or remove a symlink that resolves outside the workspace. A prompt can mention at most five unique files. A document preview is incomplete Use with a narrower line or page range. PDF and Office page requests return at most ten pages. Install LibreOffice or set when Office conversion is unavailable. A shortcut does not work Run the show-keybindings chord and inspect global and project JSON overrides. Terminal encoding may collapse into Enter or hide ; try on macOS or elsewhere. An MCP server is missing Check its command and arguments outside Agav, then inspect . MCP startup failures are non-fatal, and configuration changes require a restart. A scheduled task did not run Agav must be running when the cron expression matches. For unattended scheduling, invoke or from the operating system or CI scheduler. Terminal rendering is broken Use a modern terminal with raw input and color support. Non-interactive environments should use or instead of the Ink UI. Gather diagnostics Run to inspect provider state, model, effort, sandbox backend, loaded tools, plugins, MCP connections, context use, and token accounting.",
    "slug": "reference/troubleshooting"
  },
  {
    "id": "workflows/automation",
    "title": "Automation",
    "content": "Automation Repeat a prompt | Command | Intent | | --- | --- | | | Re-run a prompt in this session at the given interval. | | | Start a loop using the default 10 minute interval. | | | Stop the active in-session loop. | Use for an in-session interval: Supported interval suffixes are , , and . If you omit a suffix, Agav assumes minutes. Only one loop is active at a time, and starting a new loop replaces the old one. with no arguments shows the current prompt, interval, tick count, and elapsed time. Loops stop when the session exits. Schedule persistent tasks | Command | Intent | | --- | --- | | | Save a recurring task using a five-field cron schedule. | | | Show saved scheduled tasks and their state. | | | Pause the scheduled task identified by . | | | Resume the scheduled task identified by . | | | Delete the scheduled task identified by . | Scheduled tasks use standard five-field cron expressions and persist in : Cron fields support wildcards (), lists (), ranges (), and steps (). currently validates only that the cron has five fields, so malformed numeric values can still be saved but will never match. Task IDs can be given as full IDs or prefixes. The scheduler checks enabled tasks only while Agav is running in the interactive UI. It polls every 30 seconds and runs matching tasks at most once per minute. It does not install an operating-system daemon, so Agav must be active when a schedule matches. Watch files | Command | Intent | | --- | --- | | | Run when files under the path or glob change. | | | Show the active watcher. | | | Stop the active watcher. | Run a trusted, non-interactive shell command once before automating it. Then watch a path or glob: Only one watcher can be active; starting another replaces it. Changes are debounced by 300ms. Changes under , , and are ignored. Watched commands run with a 30 second timeout and write output to stderr with a prefix. Watch mode ends when Agav exits. Watch commands currently launch directly through rather than through the normal sandbox and confirmation flow, so configure only commands you trust. Avoid commands that deploy, modify data, or require interactive input. For headless jobs, prefer or from your existing CI scheduler.",
    "slug": "workflows/automation"
  },
  {
    "id": "workflows/files-and-context",
    "title": "Files and Context",
    "content": "Files and Context Mention files in prompts | Command or syntax | Intent | | --- | --- | | | Attach a cwd-relative file or directory to the prompt. | | | Attach a cwd-relative path whose name contains spaces. | | | Enter a literal without triggering a file mention. | Use cwd-relative references in interactive prompts, queries, and non-interactive modes: Agav resolves within the current working directory only. Absolute paths are rejected, and paths cannot escape through or symlinks. Agav supports at most five unique resolved paths per prompt; mentioning the same file more than once still counts as one attachment. Type for a literal . Text files are included directly when they are small enough. If a mentioned text file exceeds 500 lines or 100KB, Agav replaces the full contents with a structure outline and warning instead of silently flooding model context. In the terminal UI, paths support Tab completion, including directory completion with a trailing . Directory mentions are supported through only, not through . Directory context is non-recursive: Agav inspects immediate files only, includes at most 20 files, and shows at most 100 lines per text file. Documents and images The tool and file mentions understand: - text files, with optional inclusive line ranges - images, returned as compressed visual previews - PDFs, with inclusive page ranges of up to ten pages - Word and PowerPoint documents (, , , ) For text files, accepts absolute or relative file paths. Without a line range, text reads are limited to 1MB; ranged reads can still be truncated at 1MB with a warning. LibreOffice improves Office handling when it is installed locally, and you can point Agav to it with . Legacy and files require LibreOffice. Without LibreOffice, and fall back to text extraction only; keeps slide-based ranges, but does not preserve page ranges or layout. Vision content is useful only when the selected model can process images. Paste content Large text pastes become numbered attachments so the prompt stays readable. Text of roughly 50 or more characters is converted into a pasted attachment; shorter text stays inline. Paste an image from the clipboard to attach it when the terminal and operating system integration supports clipboard image extraction. Clipboard images are currently saved under in the working directory and rely on macOS clipboard helpers such as or . Backspace immediately after an attachment label removes it before submission. Ask a side question | Command | Intent | | --- | --- | | | Start a short, independent background question without blocking the active task. | | | Ask an independent question with a specific local file attached. | | | Change the active task instead of asking an independent side question. | Use for a small, independent question while the main agent is working: supports the same expansion as a normal prompt and receives the active conversation as read-only context. It still runs as one separate provider call rather than through the main agent loop: it cannot use tools, plugins, MCP servers, skills, or project instructions. Attach a file when the side question needs exact local context. Its answer is intentionally brief, usually one to three sentences. Use instead when the new information should change the active task. Use a normal follow-up when the question depends on the shared conversation or needs a detailed answer.",
    "slug": "workflows/files-and-context"
  },
  {
    "id": "workflows",
    "title": "Workflows",
    "content": "Workflows Agav supports short questions and longer SDLC work from the same terminal interface. - Files and context: attach code, documents, images, and pasted content. - Sessions and memory: resume sessions, fork conversations with , compact context, export chats, and preserve project knowledge. - Planning and steering: structure multi-step work and redirect an active task. - Automation: loop prompts, schedule tasks, and watch files. - Non-interactive and CI: run one-shot prompts with controlled stdout and permissions. For sensitive repositories, begin interactive work in read-only mode and move to normal confirmations when you are ready to make changes: For headless or CI-style audits, prefer an explicit tool policy:",
    "slug": "workflows"
  },
  {
    "id": "workflows/non-interactive",
    "title": "Non-interactive and CI",
    "content": "Non-interactive and CI Agav provides two one-shot modes. Quote prompts that contain spaces. In print mode, put flags such as and before , because the argument immediately after is treated as the prompt. Both modes also accept piped stdin. When stdin is present, Agav wraps it as: and prepends it to the prompt it sends to the model. Print mode | Command | Intent | | --- | --- | | | Run one agent turn and write only the final answer to stdout. | | | Stream response text while preserving stderr for tool progress and errors. | runs one agent turn, writes the final answer to stdout, writes tool progress and errors to stderr, and exits: Print mode accepts cwd-relative mentions, but it does not add dynamic repository context such as git state, project instructions, memories, or skills. Run mode | Command | Intent | | --- | --- | | | Run a one-shot, repository-aware agent task with streamed output. | | | Limit the number of internal model/tool iterations available to the task. | | | Supply an explicit JSON tool-permission policy. | includes dynamic repository context and streams output, making it suitable for agent-style CI work: Non-interactive runs auto-accept tools by default, so use or to narrow what CI jobs can do. Control tools with JSON permissions or : An explicit creates a true allowlist. If a policy contains deny rules without , Agav falls back to broad -style blocking rather than fine-grained per-tool deny behavior. and apply to only, not to . You can also use the normal selection flags in non-interactive mode, including , , , , and the Ollama connection flags. Structured output | Command | Intent | | --- | --- | | | Require the final answer to match a JSON Schema file in the current directory. | | | Require the final answer to match an inline JSON Schema. | Require final stdout to match JSON Schema: is print-mode only. Schema mode buffers output even when is present. Agav validates JSON, retries once with correction instructions, then prints only the validated JSON value. Invalid schemas, provider errors, or output that remains invalid return exit code ; success returns . Keep secrets in environment variables and use an appropriately narrow tool policy for CI jobs.",
    "slug": "workflows/non-interactive"
  },
  {
    "id": "workflows/planning-and-steering",
    "title": "Planning and Steering",
    "content": "Planning and Steering Plans | Command or prompt | Intent | | --- | --- | | | Require Agav to create a structured plan before it starts the request. | | | Show this session's plan, its step states, and recorded verification commands. | | | List every plan saved for the current project, across all sessions. | | | Manually set step to , , , or . | | | Remove this session's plan when it is no longer useful. | | or | Ask Agav to execute a request directly without creating a plan. | | | Toggle the plan detail panel to see descriptions and verify commands mid-run. | Prefix a request with to force a structured plan: Agav can also create a plan automatically for sufficiently complex refactors, migrations, or multi-phase work. Auto-generated plans usually contain 2 to 5 high-level steps. Plans are saved per-session under , anchored at the repository root. Agav works one step at a time, with step states , , , and . Plans belong to the session that created them and survive restarts. Resume a session with to pick its plan back up. Use to see all plans saved for the current project. Stale plans are automatically pruned after 30 days. and are explicit opt-ins. Agav also recognizes opt-outs such as , , , , and . Use to manually update a step, or press to view the full plan detail panel. Steering | Command | Intent | | --- | --- | | | Add as session-scoped direction for the active task. | | | Show the active steering directives. | | | Remove the directive at . | | | Remove every active steering directive. | Steers add session-scoped direction to the active task without rewriting the original request: Use steering when constraints change mid-task, a deadline narrows the scope, or a subagent also needs the updated direction. Steers are included in subsequent agent and subagent prompts until removed. They are in-memory only: they are not written to disk or resumed in a later CLI process. Running bare behaves like . Independent subagents Agav may delegate self-contained work to parallel subagents. Each gets its own conversation context and the available tools, while confirmations remain visible in the main UI. Agav currently allows up to five concurrent subagents. Write-oriented subagents attempt to work in isolated Git worktrees and apply their changes back afterward; if the merge-back step fails, Agav reports a warning. See plugins and subagents for details.",
    "slug": "workflows/planning-and-steering"
  },
  {
    "id": "workflows/sessions-and-memory",
    "title": "Sessions and Memory",
    "content": "Sessions and Memory Saved sessions | Command | Intent | | --- | --- | | | Open an interactive picker for a saved session at startup. | | | Resume the saved session whose ID starts with . | | | List saved sessions, or resume one by ID or prefix match. | | | Find saved sessions that match . | | | Give the active session a recognizable name. | | or | Start a clean chat while keeping saved sessions. | | | Export the active conversation as Markdown. | | | Save the current session and exit. | Agav auto-saves interactive conversations after each completed turn under . opens a picker with up to 20 recent sessions. You can navigate with or , press to resume, to delete, or to rename, and or to cancel. lists the 10 most recent sessions in newest-first order. performs a case-insensitive substring search across saved message text and string tool results, returns up to 10 matching sessions, and shows up to 3 snippets per session. To reopen a result directly, use its ID prefix with . Fork a conversation with | Command | Intent | | --- | --- | | | List branches in the active conversation's lineage. | | | Fork the active conversation, save the fork as , and switch to it. | Use a branch when you want to explore a different solution without losing the current conversation. The new conversation keeps the context up to the fork point, then develops independently. saves a fork from the active conversation and switches to it. Run bare to list branches in the current conversation lineage. Unrelated saved sessions are not included; use for those. A practical pattern is to branch before a risky refactor, compare the result with the original approach, and resume whichever conversation you want to continue. Context compaction | Command | Intent | | --- | --- | | | Summarize older conversation context now to make room for continued work. | | | Show a token breakdown for system prompt, tools, skills, MCP, and messages. | Agav automatically compacts long histories and preserves a summary of important decisions, paths, bugs, and preferences. Run to force compaction. Use for a real context-window breakdown. When a compaction summary exists, press in the UI to show or hide it. Project memory | Command | Intent | | --- | --- | | | Save as a durable project memory. | | | List the project's saved memories. | | | Save as a project memory. | | | Remove the saved memory named . | | | Show the active project's memory directory. | | | List the saved memories. | | | Remove the saved memory named . | | | Remove every memory for the active project. | Memories persist facts that should survive sessions, such as user preferences, corrections, project decisions, and references. They are scoped by Git repository root when available, otherwise by the current working directory. Agav stores them under and injects them into the system prompt on later turns. The model can also call when it detects durable context. Replace with the name shown by or ; for example, . Use only when you intend to remove all memories for the project.",
    "slug": "workflows/sessions-and-memory"
  }
]